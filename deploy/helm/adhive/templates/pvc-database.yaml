{{- if and .Values.persistence.enabled .Values.persistence.database.enabled -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "adhive.databasePVC" . }}
  labels:
    {{- include "adhive.labels" . | nindent 4 }}
    component: database
spec:
  accessModes:
    - {{ .Values.persistence.accessMode | quote }}
  {{- if .Values.persistence.storageClass }}
  storageClassName: {{ .Values.persistence.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.database.size | quote }}
{{- end }}
